[package]
name = "load_tester"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.18", features = ["cargo"] }
colored = "2.1.0"
mio = { version = "1.0.2", features = ["os-poll", "net"] }
regex = "1.10.6"

